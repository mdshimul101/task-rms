import React from "react";
import Slider from "../Slider/Slider";
import TopCards from "../TopCard/TopCards";

const Home = () => {
  return (
    <div>
      <Slider></Slider>
      <TopCards></TopCards>
    </div>
  );
};

export default Home;
